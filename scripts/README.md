# FacultyIQ Automation Scripts

Contains developer productivity tools, deployment scripts, database migration utilities, and health verification helpers.

## 🛠 Available Tools

- `dev.ps1`: PowerShell development lifecycle manager (`start`, `stop`, `build`, `clean`, `migrate`, `health`).
- `dev.sh`: Bash counterpart for Linux/macOS environments.
